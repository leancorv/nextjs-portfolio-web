export const metadata = {
    title: "Leandro Corvalan - Proyectos",
    description: "Mis proyectos"
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}